# DeskBox 1.2.4

## English

DeskBox 1.2.4 is a hotfix for the in-app update installation flow. It addresses the case where DeskBox could download an update, show the install confirmation, exit, and then appear to do nothing.

### Download

- [DeskBox_Setup_1.2.4_x64.exe](https://github.com/Tianyu199509/DeskBox/releases/download/v1.2.4/DeskBox_Setup_1.2.4_x64.exe)

SHA256:

```text
8ECB3092AE5BD6883F8A75BBEA03D9800251E0C23FDBE1BF4D91FD3A62565561
```

### Fixes

- Runs `DeskBox.Updater.exe` from a detached local update-helper directory before starting the installer, so the installer can safely overwrite the DeskBox install directory.
- Updates the installer packaging rule so existing `DeskBox.Updater.*` files are not overwritten while an old updater process is still running.
- Keeps the existing update manifest, SHA-256 verification, and restart-after-install behavior.

### Notes For Testing

- Check for updates from DeskBox 1.2.3 or earlier.
- Download the update, click install, confirm the dialog, and verify the installer continues after DeskBox exits.
- Confirm existing widgets, settings, Todo data, Quick Capture data, and managed storage content remain intact after overwrite install.

## 中文

DeskBox 1.2.4 是一次应用内更新热修版本，主要修复“能检查到更新、也能下载，但点击安装后弹出确认框，DeskBox 退出后没有继续安装”的问题。

### 下载

- [DeskBox_Setup_1.2.4_x64.exe](https://github.com/Tianyu199509/DeskBox/releases/download/v1.2.4/DeskBox_Setup_1.2.4_x64.exe)

SHA256：

```text
8ECB3092AE5BD6883F8A75BBEA03D9800251E0C23FDBE1BF4D91FD3A62565561
```

### 修复

- 安装更新前，会先把 `DeskBox.Updater.exe` 复制到本地更新缓存目录，再从缓存目录启动，避免更新助手锁住 DeskBox 安装目录。
- 调整安装包规则，旧版本通过应用内更新安装新版时，不再覆盖正在运行的 `DeskBox.Updater.*` 文件。
- 保留现有更新清单、SHA-256 校验和安装后重启 DeskBox 的流程。

### 测试建议

- 从 DeskBox 1.2.3 或更早版本检查更新。
- 下载更新后点击安装，确认弹窗后，检查 DeskBox 退出后安装器是否继续运行。
- 覆盖安装后确认已有格子、设置、待办数据、随记数据和收纳目录内容都能保留。
